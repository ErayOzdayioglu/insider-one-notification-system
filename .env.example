# Webhook.site UUID — obtain from https://webhook.site
WEBHOOK_UUID=your-webhook-uuid-here

# PostgreSQL (override docker-compose defaults if needed)
# POSTGRES_USER=notification
# POSTGRES_PASSWORD=notification
# POSTGRES_DB=notification

# Application
# LOG_LEVEL=info
# SERVER_PORT=8080
